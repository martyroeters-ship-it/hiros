export const HOME_HEADER_TOP_OFFSET = 40;
export const HOME_HERO_TOP_PADDING = 6;
/** Shared left/right inset so homepage content lines up with the logo on every screen. */
export const HOME_PAGE_GUTTER_CLASS = "px-6 sm:px-10 lg:px-16";
/** Break out of the page gutter on small screens so the next card can peek in. */
export const HOME_PAGE_PEEK_CAROUSEL_CLASS =
  "-mx-6 snap-x snap-mandatory overflow-x-auto overscroll-x-contain scroll-px-6 px-6 [scrollbar-width:none] [-webkit-overflow-scrolling:touch] [&::-webkit-scrollbar]:hidden sm:-mx-10 sm:scroll-px-10 sm:px-10 lg:mx-0 lg:overflow-visible lg:scroll-px-0 lg:px-0 lg:snap-none";
